import Foundation
import PostHog

public struct EzeApiWrapper {
    public init() {}
    
    // This wrapper ensures PostHog dependency is used
    public func setupPostHog() {
        // PostHog setup can be done here if needed
    }
} 